# Advanced Stats CI note

This branch is intentionally long-lived and must remain a draft/WIP until all Advanced Stats phases are complete and earlier user-prioritized branches have landed on `master`.

A draft pull request may be used only to exercise the repository CI suite during implementation. Passing CI does not change merge readiness by itself.

See `docs/advanced-stats-phase-status.md` for the authoritative merge status vocabulary and gates.
